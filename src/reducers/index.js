import { combineReducers } from 'redux';
import AddNewLogDialogVisibility from './AddNewLogDialogVisibility';

const root_reducer = combineReducers({ AddNewLogDialogVisibility, });

export default root_reducer;